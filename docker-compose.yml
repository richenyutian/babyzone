services:
  baby-steps:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: baby-steps
    ports:
      - "8080:8080"
    environment:
      APP_DATA_ROOT: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
